{
  "name": "my_notes_app",
  "version": "0.1.0",
    "private": true,
    "dependencies": {
	"@testing-library/jest-dom": "^5.17.0",
	"@testing-library/react": "^13.4.0",
	"@testing-library/user-event": "^13.5.0",
	"firebase": "^10.4.0",
	"react": "^18.2.0",
	"react-dom": "^18.2.0",
	"react-scripts": "5.0.1",
	"web-vitals": "^2.1.4"
    },
    "devDependencies": {
	"@babel/plugin-proposal-private-property-in-object": "^7.11.0",
        "jest": "^29.6.4",
        "nodemon": "^3.0.1",
        "supertest": "^6.3.3"
    },
  "scripts": {
      "start": "npm install && NODE_ENV=production react-scripts start",
      "development": "npm install && NODE_ENV=development react-scripts start",
    "build": "npm install && NODE_ENV=production react-scripts build",
    "test": "npm install && NODE_ENV=test react-scripts test",
    "eject": "npm install && react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
